public class MaxApples
{
    //Find max sum of 2 subarrays of lengths x and y that do NOT intersect
    public static int maxApples(int[] a, int x, int y)
    {
        if(a.length < x + y)
        {
            return -1;
        }

        //Placeholder
        return 0;
    }
}
